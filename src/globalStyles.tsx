import { StyleSheet } from 'react-native';
const globalStyles = StyleSheet.create({
  btnText: {
    color: '#FFD428',
    fontSize: 20,
  },
  btn: {
    width: '80%',
    marginBottom: 20,
    padding: 5,
    borderRadius: 15,
  },
});

export default globalStyles;
